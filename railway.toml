[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run railway:start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
RAILWAY_PLATFORM_ONLY = "true"